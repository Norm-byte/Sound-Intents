name: harmony_admin
description: Admin Web for Harmony by Intent
publish_to: none
version: 0.0.1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^4.0.0
  cloud_firestore: ^6.0.0
  firebase_auth: ^6.0.0
  firebase_storage: ^13.0.0
  cloud_functions: ^6.0.0
  file_picker: ^8.0.3
  shared_preferences: ^2.3.2
  url_launcher: ^6.3.0
  video_player: ^2.10.1
  intl: ^0.19.0
  syncfusion_flutter_pdfviewer: ^32.1.21
  random_string: ^2.3.1
  async: ^2.13.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
